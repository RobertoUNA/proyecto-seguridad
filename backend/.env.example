# Copia este archivo a `.env` y sustituye los valores reales.
# Nunca se deben subir secretos reales a git (`.env` está en .gitignore).

# ---- PostgreSQL / PostGIS ----
DB_HOST=localhost
DB_PORT=5432
DB_USER=tu_usuario
DB_PASSWORD=cambia_por_tu_password
DB_NAME=seguridad_vial_db

# ---- Backend ----
PORT=3000